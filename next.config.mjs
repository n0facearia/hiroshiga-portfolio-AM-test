/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow local images from public/ — the default, explicit for clarity
    unoptimized: false,
  },
}

export default nextConfig
